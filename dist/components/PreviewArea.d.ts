import { ViewMode } from '../types';

interface PreviewAreaProps {
    viewMode: ViewMode;
    children: React.ReactNode;
}
export default function PreviewArea({ viewMode, children }: PreviewAreaProps): import("react/jsx-runtime").JSX.Element;
export {};
